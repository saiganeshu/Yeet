import { Text } from "react-native";

const Footer = () => {
    return (
        <Text className="text-center text-gray-600">
            Footer component
        </Text>
    );
}

export default Footer;
